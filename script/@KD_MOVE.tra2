@音声参照
--param:100
local index,ratio = math.modf(obj.getpoint("index"))
local st = obj.getpoint(index);
local ed = obj.getpoint(index+1);

local buf = {}
local index,ratio = math.modf(obj.getpoint("index"))

num,ind = 1,1
buf = {}
obj.getaudio(buf,"audiobuffer","spectrum",num)

ret = st + ed * (buf[ind] / 8192) * (obj.getpoint("param") / 100)

return ret
